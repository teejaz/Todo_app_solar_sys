version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build needed for static site"
    build:
      commands:
        - echo "Copying files"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []